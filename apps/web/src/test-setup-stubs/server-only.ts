// Stub for the `server-only` package used by Next.js server modules.
// In unit-test environments the Next.js runtime is absent, so the real
// package would throw.  This empty export satisfies the import without
// side effects.
export {};

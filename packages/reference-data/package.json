{
  "name": "reference-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Build-time static StarStats reference catalogue (vehicle/weapon/item/location) from committed JSON snapshots, plus the single source of truth for reference-data attribution (CC BY-SA Star Citizen Wiki + CIG/RSI Ship Matrix). Additive companion to the runtime /v1/reference/* path in apps/web.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./attribution": "./src/attribution.ts",
    "./loader": "./src/loader.ts",
    "./types": "./src/types.ts"
  },
  "files": [
    "src",
    "snapshots"
  ],
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.11"
  }
}
